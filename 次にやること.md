# 次にやること

## 写真投稿自動化セットアップ

### 現状
- `scripts/photo_submission_automation.gs` は作成済み
- Apps Script プロジェクト（無題のプロジェクト）は空の状態で、コードが投入できていない

### 課題
ブラウザ操作でのコード投入に3回失敗：
1. Markdown コードブロック出力：内容が途中で切れて破損
2. テキスト貼り付け出力：同様に内容が破損
3. ブラウザ computer アクション での直接入力：コードが実際には反映されず

### 次回の対応方法（優先度順）
1. **手動コピペ**: ユーザーが `scripts/photo_submission_automation.gs` をエディタで直接開き、全コードをコピペする（最も確実）
2. **clasp デプロイ**: Google 公式の `clasp` CLI ツールを使用してコードをデプロイ
   ```bash
   npm install -g @google/clasp
   clasp login
   clasp create --type webapp
   clasp push
   ```

### 完了待ちのセットアップ
Apps Script エディタにコードが入れば、以下を実行：
1. Airtable トークン設定：`setAirtableToken()` 実行
2. トリガー設定：`onFormSubmit` → フォーム送信時
3. テスト実行（実行前に別途確認）

### バックアップ済みデータ
テスト用3店舗の Photos/Logo フィールド値：
- **binn**: 空
- **ナミテテ**: `https://drive.google.com/file/d/1xIFZMjUZm3lJn255ACi_iCpI4lJ_XaTa/view?usp=sharing`
- **HARUMACHI coffee**: `https://drive.google.com/uc?id=1nWi1aJKvMx34i3ori6sb2Sqb2kuBHfW&export=view`
